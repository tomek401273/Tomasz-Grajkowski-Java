
DROP PROCEDURE IF EXISTS ShowUser;

DELIMITER $$

CREATE PROCEDURE ShowUser(id INT)
BEGIN
DECLARE ERROR VARCHAR(30) DEFAULT "SOMETHIG SOMETHING GO WRONG";

    IF id <= 0 THEN
    SELECT ERROR;
    ELSE
	SELECT * FROM READERS WHERE READER_ID = id;
    END IF;

END $$

DELIMITER ;

CALL ShowUser(0);